apiVersion: apps/v1
kind: Deployment
metadata:
  name: interaction-service
  labels:
    app: interaction-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: interaction-service
  template:
    metadata:
      labels:
        app: interaction-service
    spec:
      containers:
        - name: interaction-service
          image: INTERACTION_IMAGE
          ports:
            - containerPort: 3002
          resources:
            requests:
              memory: '64Mi'
              cpu: '100m'
            limits:
              memory: '128Mi'
              cpu: '250m'
          env:
            - name: PORT
              value: '3002'
            - name: DATABASE_HOST
              valueFrom:
                secretKeyRef:
                  name: interaction-service-external-secret
                  key: DATABASE_HOST
            - name: DATABASE_PORT
              valueFrom:
                secretKeyRef:
                  name: interaction-service-external-secret
                  key: DATABASE_PORT
            - name: DATABASE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: interaction-service-external-secret
                  key: DATABASE_USERNAME
            - name: DATABASE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: interaction-service-external-secret
                  key: DATABASE_PASSWORD
            - name: DATABASE_NAME
              valueFrom:
                secretKeyRef:
                  name: interaction-service-external-secret
                  key: DATABASE_NAME
      restartPolicy: Always
