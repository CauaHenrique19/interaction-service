apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - host: my-app.local
      http:
        paths:
          - path: /interaction-service
            pathType: Prefix
            backend:
              service:
                name: interaction-service
                port:
                  number: 3001
